NVIM is a terminal based text/code editor based on **vim**

Thing is, vim was good, but the configurations were all made in *vimscript* which was apparently a pain in the ass. **NVIM** allows you to configure it using **Lua** which is way simpler.

