To understand [[Docker]], it's important to understand it's **Architecture**, which means how the different parts of docker all work together to bring you [[Docker]].

**Docker uses a client-server architecture.**
*means it has a client component, which communicates with a server component using a [[APIs#REST API|REST API]]*
